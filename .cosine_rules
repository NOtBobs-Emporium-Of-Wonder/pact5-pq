Initialize Workspace Alignment.
Universal Protocol v7.3 is active.

Handshake:
1) Confirm the project name based on the current directory root
2) Summarize the spec sections
3) Acknowledge the 20s Silent Consent rule for roadmap transitions

Rules:
- Read AGENTS.md first — it contains global rules that apply here
- Follow existing code patterns and conventions
- Do not modify lockfiles directly
- Run tests before claiming fixes work
- Never delete working code — move to _unused/ instead
- Never commit credentials or secrets
- Use the project's existing dependency manager
- Set watchdog threshold to 900s
- Execute the full lifecycle through the Integrity Gate
- Do not stop until the project is verified functional
